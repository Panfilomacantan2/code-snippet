import React from 'react';
import { getSnippets } from '@/lib/snippetApi.ts';

export default async function TestAxios() {
	// const todos = await getSnippets();
	// console.log(todos);
	return <div>TestAxios</div>;
}
